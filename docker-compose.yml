services:
  whatsapp:
    image: aldinokemal2104/go-whatsapp-web-multidevice
    container_name: whatsapp-gateway
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - whatsapp_storages:/app/storages
    command:
      - rest
      - --basic-auth=${WHATSAPP_GATEWAY_USERNAME:-admin}:${WHATSAPP_GATEWAY_PASSWORD:-admin}
      - --port=3000
      - --webhook=${WHATSAPP_WEBHOOK:-}
      - --webhook-secret=${WHATSAPP_WEBHOOK_SECRET:-secret}

  cek-pelunasan:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cek-pelunasan-app
    restart: unless-stopped
    ports:
      - "${SERVER_PORT:-8080}:8080"
    env_file:
      - .env
    environment:
      # Override internal WhatsApp URL agar pakai Docker network
      - WHATSAPP_GATEWAY_URL=http://whatsapp-gateway:3000
    deploy:
      resources:
        limits:
          memory: 512M
    depends_on:
      - whatsapp

volumes:
  whatsapp_storages:
